func newFunction(a, b) {
    print(a);
    print(b);
    func nestedFunction() {
       return a + b;
    }
    print("a + b is ...");
    return nestedFunction();
}

print(newFunction(43, 22));
let alpha = newFunction;
print(alpha(32, 12));


func lambdaTest(a, b) {
    return func (a, b) {
               return a + b;
           };
}

print(lambdaTest(54, 66));

let alpha = lambdaTest;
print(alpha(15, 15));
